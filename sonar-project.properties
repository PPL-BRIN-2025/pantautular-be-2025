sonar.projectKey=PPL-BRIN-2025_pantautular-be-2025
sonar.organization=ppl-brin-2025
sonar.sourceEncoding=UTF-8

# ✅ Only scan admin_feature folder
sonar.sources=admin_feature

# ✅ Only look for tests in admin_feature (even if we skip them for now)
sonar.tests=admin_feature
sonar.test.inclusions=**/test_*.py,**/*_test.py,**/tests/**/*.py,**/tests.py

# ✅ Coverage report (can be empty for now)
sonar.python.coverage.reportPaths=coverage.xml

# ✅ Instead of excluding everything, explicitly exclude other folders
sonar.exclusions=pt_backend/**,pantau_tular/**,authentication/**,hello/**,curator_feature/**,**/migrations/**,**/__pycache__/**

# ✅ Coverage also only applies to admin_feature
sonar.coverage.exclusions=pt_backend/**,pantau_tular/**,authentication/**,hello/**,curator_feature/**,**/migrations/**,**/tests/**,**/settings.py
